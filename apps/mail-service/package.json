{
  "name": "mail-service",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/handler.ts --outdir dist --target node && cd dist && zip handler.zip handler.js",
    "dev": "bun run --watch src/handler.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  }
}
