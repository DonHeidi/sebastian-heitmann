---
import type { Strings } from '../i18n/types';
import { locales, localeConfig, type Locale } from '../i18n/utils';
import { getRelativeLocaleUrl } from 'astro:i18n';

type Props = {
    nav?: Strings['nav'];
    languagePicker?: Strings['languagePicker'];
};

const { nav: navProp, languagePicker } = Astro.props;
const nav = navProp ?? { logo: 'sebastian-heitmann', logoDot: '.dev', cvLabel: 'CV', cta: 'Book a Call' };
const currentLocale = (Astro.currentLocale ?? 'en-us') as Locale;
const currentConfig = localeConfig[currentLocale];

// Extract the page path without the locale prefix
const pathname = Astro.url.pathname;
const pagePath = pathname.replace(/^\/(de-de|en-us)(\/|$)/, '/').replace(/\/$/, '') || '/';
---

<nav class="nav">
    <a class="nav-logo" href="/">{nav.logo}<span class="nav-logo__dot">.</span>dev</a>
    <div class="nav-actions">
        <div class="nav-lang" role="navigation" aria-label={languagePicker?.label ?? 'Language'}>
            {locales.map((loc) => (
                <a
                    class:list={['nav-lang__link', { 'nav-lang__link--active': loc === currentLocale }]}
                    href={getRelativeLocaleUrl(loc, pagePath)}
                    data-locale={loc}
                    aria-current={loc === currentLocale ? 'page' : undefined}
                >
                    {localeConfig[loc].label}
                </a>
            ))}
        </div>
        <div class="nav-theme-toggle" role="radiogroup" aria-label="Theme">
            <button class="nav-theme-toggle__btn" type="button" data-theme="system" aria-label="System theme">
                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
                    <rect x="2" y="3" width="20" height="14" rx="2" ry="2"></rect>
                    <line x1="8" y1="21" x2="16" y2="21"></line>
                    <line x1="12" y1="17" x2="12" y2="21"></line>
                </svg>
            </button>
            <button class="nav-theme-toggle__btn" type="button" data-theme="light" aria-label="Light theme">
                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
                    <circle cx="12" cy="12" r="5"></circle>
                    <line x1="12" y1="1" x2="12" y2="3"></line>
                    <line x1="12" y1="21" x2="12" y2="23"></line>
                    <line x1="4.22" y1="4.22" x2="5.64" y2="5.64"></line>
                    <line x1="18.36" y1="18.36" x2="19.78" y2="19.78"></line>
                    <line x1="1" y1="12" x2="3" y2="12"></line>
                    <line x1="21" y1="12" x2="23" y2="12"></line>
                    <line x1="4.22" y1="19.78" x2="5.64" y2="18.36"></line>
                    <line x1="18.36" y1="5.64" x2="19.78" y2="4.22"></line>
                </svg>
            </button>
            <button class="nav-theme-toggle__btn" type="button" data-theme="dark" aria-label="Dark theme">
                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"></path>
                </svg>
            </button>
        </div>
        <a class="nav-link" href={getRelativeLocaleUrl(currentLocale, 'cv')}>{nav.cvLabel}</a>
        <a class="nav-cta" href={`${getRelativeLocaleUrl(currentLocale, '/')}#contact`}>{nav.cta}</a>
    </div>
</nav>

<script>
    function applyTheme(preference: string) {
        const html = document.documentElement;
        html.classList.remove('dark', 'light');
        if (preference === 'dark' || preference === 'light') {
            html.classList.add(preference);
        } else {
            // system
            const systemDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
            html.classList.add(systemDark ? 'dark' : 'light');
        }
    }

    function updateActiveBtn(preference: string) {
        document.querySelectorAll('.nav-theme-toggle__btn').forEach(btn => {
            const isActive = (btn as HTMLElement).dataset.theme === preference;
            btn.classList.toggle('nav-theme-toggle__btn--active', isActive);
        });
    }

    // Set initial active state
    const stored = localStorage.getItem('theme') || 'system';
    updateActiveBtn(stored);

    // Handle clicks
    document.querySelectorAll('.nav-theme-toggle__btn').forEach(btn => {
        btn.addEventListener('click', () => {
            const preference = (btn as HTMLElement).dataset.theme!;
            localStorage.setItem('theme', preference);
            applyTheme(preference);
            updateActiveBtn(preference);
        });
    });

    // Listen for OS theme changes when in system mode
    window.matchMedia('(prefers-color-scheme: dark)').addEventListener('change', () => {
        const current = localStorage.getItem('theme') || 'system';
        if (current === 'system') {
            applyTheme('system');
        }
    });
</script>

<script>
    document.querySelectorAll('.nav-lang__link').forEach(link => {
        link.addEventListener('click', () => {
            const locale = (link as HTMLElement).dataset.locale;
            if (locale) {
                localStorage.setItem('locale', locale);
            }
        });
    });
</script>

<style lang="scss">
    .nav {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 28px 80px;
        max-width: 1440px;
        margin: 0 auto;

        &-logo {
            font-family: var(--v8-font-mono);
            font-size: 13px;
            letter-spacing: 0.02em;
            color: var(--v8-text-muted);
            text-decoration: none;
            transition: color 0.2s ease;

            &:hover {
                color: var(--v8-text);
            }

            &__dot {
                color: var(--v8-accent);
            }
        }

        &-actions {
            display: flex;
            align-items: center;
            gap: 16px;
        }

        &-lang {
            display: flex;
            border: 1px solid var(--v8-border);

            &__link {
                font-family: var(--v8-font-mono);
                font-size: 10px;
                letter-spacing: 0.1em;
                color: var(--v8-text-muted);
                text-decoration: none;
                padding: 6px 8px;
                display: flex;
                align-items: center;
                justify-content: center;
                transition: color 0.2s ease, background-color 0.2s ease;

                &:not(:last-child) {
                    border-right: 1px solid var(--v8-border);
                }

                &:hover {
                    color: var(--v8-accent);
                }

                &--active {
                    color: var(--v8-accent);
                    background: var(--v8-bg-surface);
                }
            }
        }

        &-theme-toggle {
            display: flex;
            border: 1px solid var(--v8-border);

            &__btn {
                background: none;
                border: none;
                color: var(--v8-text-muted);
                cursor: pointer;
                padding: 6px 8px;
                display: flex;
                align-items: center;
                justify-content: center;
                transition: color 0.2s ease, background-color 0.2s ease;

                &:not(:last-child) {
                    border-right: 1px solid var(--v8-border);
                }

                &:hover {
                    color: var(--v8-accent);
                }

                &--active {
                    color: var(--v8-accent);
                    background: var(--v8-bg-surface);
                }
            }
        }

        &-link {
            font-family: var(--v8-font-mono);
            font-size: 10px;
            letter-spacing: 0.1em;
            text-transform: uppercase;
            color: var(--v8-text-muted);
            text-decoration: none;
            padding: 6px 12px;
            border: 1px solid var(--v8-border);
            transition: color 0.2s ease, border-color 0.2s ease;

            &:hover {
                color: var(--v8-text);
                border-color: var(--v8-text-muted);
            }
        }

        &-cta {
            font-family: var(--v8-font-mono);
            font-size: 12px;
            letter-spacing: 0.08em;
            text-transform: uppercase;
            color: var(--v8-text);
            text-decoration: none;
            padding: 10px 24px;
            border: 1px solid var(--v8-border);
            transition: border-color 0.2s ease, color 0.2s ease;

            &:hover {
                border-color: var(--v8-accent);
                color: var(--v8-accent);
            }
        }
    }

    @media (max-width: 768px) {
        .nav {
            padding: 16px 24px;
            flex-wrap: wrap;
            gap: 12px;

            &-logo {
                font-size: 12px;
            }

            &-actions {
                gap: 10px;
            }

            &-cta {
                font-size: 10px;
                padding: 8px 14px;
                letter-spacing: 0.06em;
            }
        }
    }

    @media (max-width: 400px) {
        .nav {
            &-logo {
                width: 100%;
            }

            &-actions {
                width: 100%;
                justify-content: space-between;
            }
        }
    }
</style>
