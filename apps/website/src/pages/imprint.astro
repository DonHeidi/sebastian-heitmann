---
import Layout from '../layouts/Layout.astro';
import Navigation from '../components/navigation.astro';
import Footer from '../components/footer.astro';
import { getStrings, getHreflangAlternates } from '../i18n/utils';
import type { Locale } from '../i18n/utils';

const locale = Astro.currentLocale as Locale;
const s = getStrings(locale);
const alternates = getHreflangAlternates();
---
<Layout locale={locale} title="Imprint — Sebastian Heitmann" description="Legal notice and contact information." alternates={alternates}>
    <Navigation nav={s.nav} languagePicker={s.languagePicker} />
    <main class="legal">
        <div class="legal-inner">
            <h1 class="legal-title">Imprint</h1>
            <div class="legal-rule"></div>

            <section class="legal-section">
                <h2 class="legal-heading">Information pursuant to § 5 DDG</h2>
                <p>Sebastian Heitmann<br/>Strategic Technology Consultant<br/>Kastanienallee 24<br/>21255 Tostedt</p>
            </section>

            <section class="legal-section">
                <h2 class="legal-heading">Contact</h2>
                <p>Email: me@sebastian-heitmann.dev<br/>Contact form: <a href="/#contact">www.sebastian-heitmann.dev/#contact</a></p>
            </section>

            <section class="legal-section">
                <h2 class="legal-heading">VAT</h2>
                <p>VAT is not charged in accordance with § 19 UStG (small business regulation).</p>
            </section>

            <section class="legal-section">
                <h2 class="legal-heading">Responsible for content pursuant to § 18 (2) MStV</h2>
                <p>Sebastian Heitmann<br/>Kastanienallee 24<br/>21255 Tostedt</p>
            </section>
        </div>
    </main>
    <Footer footer={s.footer} />
</Layout>

<style lang="scss">
    .legal {
        padding: 160px 80px 120px;
        max-width: 1440px;
        margin: 0 auto;

        &-inner {
            max-width: 640px;
        }

        &-title {
            font-family: var(--v8-font-display);
            font-size: clamp(40px, 5vw, 64px);
            line-height: 1.05;
            color: var(--v8-text);
            margin-bottom: 24px;
        }

        &-rule {
            width: 48px;
            height: 2px;
            background: var(--v8-accent);
            margin-bottom: 64px;
        }

        &-section {
            margin-bottom: 48px;

            p {
                font-family: var(--v8-font-body);
                font-size: 16px;
                font-weight: 300;
                line-height: 1.8;
                color: var(--v8-text-tertiary);
            }

            a {
                color: var(--v8-accent);
                text-decoration: none;
                border-bottom: 1px solid transparent;
                transition: border-color 0.2s;

                &:hover {
                    border-bottom-color: var(--v8-accent);
                }
            }
        }

        &-heading {
            font-family: var(--v8-font-mono);
            font-size: 10px;
            letter-spacing: 0.12em;
            text-transform: uppercase;
            color: var(--v8-text-muted);
            margin-bottom: 16px;
        }
    }

    @media (max-width: 768px) {
        .legal {
            padding: 120px 24px 80px;
        }
    }
</style>
