.terraform/
*.tfstate
*.tfstate.backup
terraform.tfvars
# .terraform.lock.hcl is intentionally committed (HashiCorp best practice) for
# reproducible provider versions across machines — do not ignore it.
