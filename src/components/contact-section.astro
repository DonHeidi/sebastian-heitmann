---
import { Icon } from 'astro-icon/components';
---

<section class="contact reveal" id="contact">
    <div class="contact__header">
        <h2 class="contact__title">Let's Talk</h2>
        <p class="contact__subtitle">Tell me about your project and I'll get back to you within 24 hours with an honest assessment of how I can help.</p>
    </div>

    <form class="contact__form" action="#" method="POST">
        <div class="contact__row">
            <div class="contact__field">
                <label class="contact__label" for="contact-name">Name *</label>
                <input
                    class="contact__input"
                    type="text"
                    id="contact-name"
                    name="name"
                    placeholder="Your full name"
                    required
                />
            </div>
            <div class="contact__field">
                <label class="contact__label" for="contact-email">Email *</label>
                <input
                    class="contact__input"
                    type="email"
                    id="contact-email"
                    name="email"
                    placeholder="your@email.com"
                    required
                />
            </div>
        </div>

        <div class="contact__row">
            <div class="contact__field">
                <label class="contact__label" for="contact-company">Company</label>
                <input
                    class="contact__input"
                    type="text"
                    id="contact-company"
                    name="company"
                    placeholder="Your company"
                />
            </div>
            <div class="contact__field">
                <label class="contact__label" for="contact-plan">Engagement</label>
                <div class="contact__select-wrapper">
                    <select
                        class="contact__select"
                        id="contact-plan"
                        name="plan"
                    >
                        <option value="" disabled selected>Select an engagement</option>
                        <option value="hourly">On-Demand Advisory & Engineering</option>
                        <option value="cycle">Fixed 2-Week Cycle</option>
                        <option value="retainer">Fractional CTO Retainer</option>
                        <option value="unsure">Not sure yet</option>
                    </select>
                    <Icon name="mdi:chevron-down" class="contact__select-icon" width={20} height={20} />
                </div>
            </div>
        </div>

        <div class="contact__field">
            <label class="contact__label" for="contact-message">Message *</label>
            <textarea
                class="contact__textarea"
                id="contact-message"
                name="message"
                placeholder="Tell me about your project..."
                required
            ></textarea>
        </div>

        <button type="submit" class="contact__submit">
            Send Message
        </button>
    </form>
</section>

<style lang="scss">
    .contact {
        padding: 80px 280px 80px;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 40px;

        &__header {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 12px;
            text-align: center;
        }

        &__title {
            font-family: 'Anton', sans-serif;
            font-size: 6rem;
            font-weight: 700;
            color: var(--clr-text-primary);
            letter-spacing: -2px;
            line-height: 1;
        }

        &__subtitle {
            font-family: 'Outfit', sans-serif;
            font-size: 1.125rem;
            color: var(--clr-text-muted);
            text-align: center;
            max-width: 480px;
            line-height: 1.6;
        }

        &__form {
            display: flex;
            flex-direction: column;
            gap: 20px;
            width: 100%;
            max-width: 820px;
        }

        &__row {
            display: flex;
            flex-direction: row;
            gap: 20px;
        }

        &__field {
            display: flex;
            flex-direction: column;
            gap: 8px;
            flex: 1;
        }

        &__label {
            font-family: 'Outfit', sans-serif;
            font-size: 12px;
            font-weight: 600;
            color: var(--clr-text-primary);
        }

        &__input {
            height: 44px;
            padding: 0 16px;
            border-radius: 8px;
            background: var(--clr-bg-surface);
            border: 1px solid rgba(255, 255, 255, 0.08);
            color: var(--clr-white);
            font-family: 'Outfit', sans-serif;
            font-size: 1rem;
            outline: none;
            transition: border-color 0.2s;
            backdrop-filter: blur(12px);

            &::placeholder {
                color: rgba(255, 255, 255, 0.4);
            }

            &:focus {
                border-color: rgba(255, 255, 255, 0.2);
            }
        }

        &__select-wrapper {
            position: relative;
            display: flex;
            align-items: center;
        }

        &__select {
            height: 44px;
            padding: 0 16px;
            border-radius: 8px;
            background: var(--clr-bg-surface);
            border: 1px solid rgba(255, 255, 255, 0.08);
            color: rgba(255, 255, 255, 0.4);
            font-family: 'Outfit', sans-serif;
            font-size: 1rem;
            outline: none;
            appearance: none;
            -webkit-appearance: none;
            width: 100%;
            cursor: pointer;
            backdrop-filter: blur(12px);
            transition: border-color 0.2s;

            option {
                background: #1c1814;
                color: var(--clr-white);
            }

            option:not([value=""]) {
                color: var(--clr-white);
            }

            &:focus {
                border-color: rgba(255, 255, 255, 0.2);
            }
        }

        &__select-icon {
            position: absolute;
            right: 12px;
            pointer-events: none;
            color: rgba(255, 255, 255, 0.4);
            flex-shrink: 0;
        }

        &__textarea {
            height: 120px;
            padding: 12px 16px;
            border-radius: 8px;
            background: var(--clr-bg-surface);
            border: 1px solid rgba(255, 255, 255, 0.08);
            color: var(--clr-white);
            font-family: 'Outfit', sans-serif;
            font-size: 1rem;
            outline: none;
            resize: vertical;
            line-height: 1.5;
            transition: border-color 0.2s;
            backdrop-filter: blur(12px);

            &::placeholder {
                color: rgba(255, 255, 255, 0.4);
            }

            &:focus {
                border-color: rgba(255, 255, 255, 0.2);
            }
        }

        &__submit {
            width: fit-content;
            align-self: center;
            padding: 1rem 2.5rem;
            border-radius: 100vh;
            background: var(--clr-bg-surface);
            border: 1.5px solid var(--clr-text-primary);
            font-family: 'Anton', sans-serif;
            font-size: 1.125rem;
            line-height: 1;
            color: var(--clr-white);
            cursor: pointer;
            transition: background-color 0.2s ease, border-color 0.2s ease;

            &:hover {
                background-color: var(--clr-text-primary);
                color: #151310;
            }
        }
    }

    @media (max-width: 1440px) {
        .contact {
            padding: 80px 80px 80px;
        }
    }

    @media (max-width: 768px) {
        .contact {
            padding: 96px 2rem;

            &__row {
                flex-direction: column;
            }

            &__title {
                font-size: 2.75rem;
            }
        }
    }

    @media (max-width: 375px) {
        .contact {
            padding: 64px 1.25rem;

            &__title {
                font-size: 2.25rem;
            }

            &__subtitle {
                font-size: 1rem;
            }
        }
    }
</style>
